caching:
  spring:
    percentage:
      ttl: 5000
spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/postgres"
    username: "postgres"
    password: "123456"
    driver-class-name: "org.postgresql.Driver"
  jpa:
    show-sql: "true"
    properties:
      hibernate:
        format_sql: "true"
        ddl-auto: "create"
        hbm2ddl:
          auto: "create"